<!DOCTYPE html>
<html lang="pt-br">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="/stylesheets/global.css">
    <link rel="shortcut icon" href="/images/dashboard-aluno/favicon.png" />
    <link rel="stylesheet" href="/stylesheets/dashboard-aluno/estiloa1.css">
    <link rel="stylesheet" href="/stylesheets/landing-page/contact-us.css">
    <script defer src="/fa/brands.js"></script>
    <script defer src="/fa/solid.js"></script>
    <script defer src="/fa/fontawesome.js"></script>
    <title>elevel - Dashboard Aluno</title>
</head>
<body>
    <%- include('./partials/aside-top.ejs') %>
            <a href="/dashboard/aluno/10"><i class="fas fa-graduation-cap"></i> Aluno</a>
            <a href="/dashboard/aluno/10/turmas" class="active"><i class="fas fa-users"></i> Turmas</a>
    <%- include('./partials/aside-bottom.ejs') %>
    <div class="main">
        <h2>Dashboard / Turmas</h2>
        <hr />
        <div class="billboard">
            <a href="#menu-toggle" class="close-bilboard btn-default"><i class="fas fa-times"></i></a>
            <h3><%= student.nome %>,</h3>
            <p>Aqui você pode conferir e gerenciar todos cursos e turmas que participa! Selecione para visualizar todos os detalhes.</p>
        </div>
        <h4>Turmas que faço parte</h4>
        <div class="tables">
            <table id="data-table">
                <thead>
                    <tr>
                        <th>Código</th>
                        <th>Curso</th>
                        <th>Turma</th>
                        <th>Professor</th>
                        <th>Pontos</th>
                        <th>Ações</th>
                    </tr>
                </thead>
                <tbody>
                <% data.forEach(row => { %>
                    <tr onclick="window.location='/dashboard/aluno/<%= student.idUser %>/turmas/<%= row.Turma.idTurma %>'">
                        <td><%= row.id %></td>
                        <td><%= row.Turma.titulo %></td>
                        <td><%= row.Turma.codigo.slice(0,6) %></td>
                        <td>Ivens</td>
                        <td>33.756</td>
                        <td>
                            <i class="fas fa-trash"></i>
                        </td>
                    </tr>
                <% }) %>
                </tbody>
            </table>
        </div>
        <h4>Entrar em uma nova turma</h4>
        <div class="more-actions">
            <form class="form" method="POST" action="/dashboard/aluno/<%= student.idUser %>/turmas">
                <input class="styled-input" type="text" name="idTurma" placeholder="ID da turma" />
                <button class="submit-button" type="submit">Participar</button>
            </form>
        </div>
        <div class="notification">
            <img src="/images/dashboard-aluno/avatar.png" class="avatar" alt="avatar">
            <span class="badge">🔔</span>
        </div>
    </div>
</body>
</html>