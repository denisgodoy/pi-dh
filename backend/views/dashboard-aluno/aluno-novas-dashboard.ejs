<!DOCTYPE html>
<html lang="pt-br">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="/stylesheets/global.css">
    <link rel="shortcut icon" href="/images/dashboard-aluno/favicon.png" />
    <link rel="stylesheet" href="/stylesheets/dashboard-aluno/estiloa1.css">
    <link rel="stylesheet" href="/stylesheets/landing-page/contact-us.css">
    <script defer src="/fa/brands.js"></script>
    <script defer src="/fa/solid.js"></script>
    <script defer src="/fa/fontawesome.js"></script>
    <title>elevel - Dashboard Aluno</title>
</head>
<body>
    <%- include('../partials/aside-top.ejs') %>
            <a href="/dashboard/aluno" class="active"><i class="fas fa-graduation-cap"></i> Aluno</a>
            <a href="/dashboard/aluno/profile"><i class="fas fa-user"></i> Perfil</a>
            <a href="/dashboard/aluno/turmas" ><i class="fas fa-users"></i> Turmas</a>
    <%- include('../partials/aside-bottom.ejs') %>
    <div class="main">
        <h2>Dashboard / Aluno / Novas Atividades</h2>
        <hr />
        <div id="billboard">
            <button id="close-billboard"><i class="fas fa-times"></i></button>
            <h3><%= nome %>,</h3>
            <p>Estas são as novas atividades disponíveis para as turmas que está registrado.</p>
        </div>
        <h4>Novas atividades disponíveis</h4>
        <div class="tables">
            <table id="data-table">
                <thead>
                    <tr>
                        <th>Turma</th>
                        <th>Atividade</th>
                        <th>Descrição</th>
                        <th>Pontos</th>
                        <th>Prazo</th>
                    </tr>
                </thead>
                <tbody>
                <% for (let nova of newAct) { %>
                    <tr onclick="window.location='/dashboard/aluno/turmas/<%= nova.idTurma %>/atividades/<%= nova.idAtividade %>'">
                        <td><%= nova.idTurma %></td>
                        <td><%= nova.titulo %></td>
                        <td><%= nova.descricao %></td>
                        <td><%= nova.pontuacao %></td>
                        <td><%= nova.dataTermino %></strong></td>
                    </tr>
                <% } %>
                </tbody>
            </table>
        </div>
        <div class="notification">
            <img src="/images/dashboard-aluno/avatar.png" class="avatar" alt="avatar">
            <span class="badge">🔔</span>
        </div>
    </div>
    <script src="/js/dashboard-aluno/dashboard.js"></script>
</body>
</html>