<!DOCTYPE html>
<html lang="pt-br">

<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="/stylesheets/dashboard-professor/global.css">
    <link rel="stylesheet" href="/stylesheets/dashboard-professor/dashboard.css">
    <link rel="shortcut icon" href="/images/favicon.png" />

    <script defer src="/node_modules/@fortawesome/fontawesome-free/js/brands.js"></script>
    <script defer src="/node_modules/@fortawesome/fontawesome-free/js/solid.js"></script>
    <script defer src="/node_modules/@fortawesome/fontawesome-free/js/fontawesome.js"></script>

    <title>elevel - Dashboard Professor</title>

</head>

<body>

    <%- include('partials/aside'); %>


    <div class="main">
        <%- include('../partials/profile.ejs') %>
        <h2>Dashboard / Turmas </h2>
        <hr />
        <div class="billboard">
            <a href="#menu-toggle" class="close-bilboard btn-default"><i class="fas fa-times"></i></a>
            <h3><%= nome %> 😀</h3>
            <p></p>
        </div>
        <div class="tables">
            <table id="data-table">
                <thead>
                    <tr>
                        <th>Código</th>
                        <th>Curso</th>
                        <!-- <th>Início</th>
                        <th>Final</th> -->
                        <th>Ações</th>
                    </tr>
                </thead>
                <tbody>
                    <% for (let turma of data) { %>
                    <tr>
                        <td>
                            <a href="atividades/<%= turma.idturma%>">
                            <%= turma.codigo %>
                            </a>
                      
                        </td>
                        <td>
                            <a href="atividades/<%= turma.idturma%>">
                            <%= turma.titulo %>
                             </a>
                        </td>

                        <td>
                       <span>
                        <form method="GET" action="/dashboard/professor/turmas/<%= turma.idturma %>">
                            <a href="#"><button type="submit"><i class="fas fa-edit btn-gray"></i></button></a>
                        </form>
                        <form method="POST" action="/dashboard/professor/turmas/<%= turma.idturma %>/deletar?_method=DELETE"> 
                            <button type="submit"><a><i class="fas fa-trash btn-red"></i></a></button>
                        </form>   
                    </span>
                        </td>
                    </tr>
                    <% } %>
                </tbody>
            </table>
        </div>

        <a href="/dashboard/professor/turmas/nova" class="btn new">Nova Turma</a>

        <%- include('partials/chat'); %>

    </div>

    <script src="https://ajax.googleapis.com/ajax/libs/jquery/2.0.2/jquery.min.js"></script>
    <script src="assets/scritps/dashboard-professor/dashboard.js"></script>
</body>

</html>