<!DOCTYPE html>
<html lang="pt-br">
  <head>
    <meta charset="UTF-8" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link rel="stylesheet" href="/stylesheets/dashboard-professor/global.css" />
    <link
      rel="stylesheet"
      href="/stylesheets/dashboard-professor/dashboard.css"
    />
    <link rel="shortcut icon" href="/images/favicon.png" />

    <script
      defer
      src="/node_modules/@fortawesome/fontawesome-free/js/brands.js"
    ></script>
    <script
      defer
      src="/node_modules/@fortawesome/fontawesome-free/js/solid.js"
    ></script>
    <script
      defer
      src="/node_modules/@fortawesome/fontawesome-free/js/fontawesome.js"
    ></script>

    <title>elevel - Dashboard Professor</title>
  </head>

  <body>
    <%- include('partials/aside'); %>

    <div class="main">
      
      <%- include('../partials/aside-top.ejs') %>
      <nav>
        <a href="/dashboard/professor/profile"><i class="fas fa-graduation-cap"></i> Perfil</a>
        <a href="/dashboard/professor/atividades/<%= idAtividade %>" class="active"><i class="fas fa-file-alt"></i> Atividades</a>
        <a href="/dashboard/professor/avaliacoes"><i class="fas fa-spell-check"></i> Avaliações</a>
        <a href="/dashboard/professor/turmas"><i class="fas fa-users"></i> Turmas</a>
    </nav>
      <%- include('../partials/aside-bottom.ejs') %>

      <%- include('../partials/profile.ejs') %>
      <h2>Dashboard / Atividades / Editar</h2>
      <hr />
      <div class="billboard" style="display: none">
        <a href="#menu-toggle" class="close-bilboard btn-default"
          ><i class="fas fa-times"></i
        ></a>
        <h3>Olá, Leandro! 😀</h3>
        <p>
          Esta é a sua área de professor no Dashboard Elevel. Aqui você pode encontrar as turmas das quais faz parte, gerenciar atividades pendentes e verificar quantos pontos você possui em comparação com outras pessoas da mesma turma.
        </p>
      </div>

      <form action="/dashboard/professor/atividade/<%= idAtividade %>/alterar" method="POST">
        <div>
          <span>
            <input type="text" name="titulo" value="<%= data.titulo %>" placeholder="Titulo da Atividade" />
          </span>
          <span>
            <input type="number" name="pontuacao" value="<%= data.pontuacao %>" step="100" min="500" placeholder="Pontuação" />
          </span>
          <span>
            <input
              placeholder="Data da Entrega"
              class="textbox-n"
              name="dataTermino"
              value="<%= data.dataTermino %>"
              type="text"
              onfocus="(this.type='date')"
            />
          </span>

          <span>
            <textarea
              placeholder="Descrição da atividade"
              name="descricao"
              value="<%= data.descricao %>"
              rows="10"
              cols="77"
              style="max-width: 95%"
            ></textarea>
          </span>
        </div>

        <button type="submit" class="btn new" style="margin-left:0px !important;">Editar Atividade</button>

      </form>

    </div>

    <script src="https://ajax.googleapis.com/ajax/libs/jquery/2.0.2/jquery.min.js"></script>
    <script src="assets/scritps/dashboard-professor/dashboard.js"></script>
  </body>
</html>
