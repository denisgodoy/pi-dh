<!DOCTYPE html>
<html lang="pt-br">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="/stylesheets/global.css">
    <link rel="shortcut icon" href="/images/dashboard-aluno/favicon.png" />
    <link rel="stylesheet" href="/stylesheets/dashboard-aluno/estiloa1.css">
    <link rel="stylesheet" href="/stylesheets/landing-page/contact-us.css">
    <script defer src="/fa/brands.js"></script>
    <script defer src="/fa/solid.js"></script>
    <script defer src="/fa/fontawesome.js"></script>
    <title>elevel - Dashboard Aluno</title>
</head>
<body>
    <%- include('./partials/aside-top.ejs') %>
            <a href="/dashboard/aluno/10"><i class="fas fa-graduation-cap"></i> Aluno</a>
            <a href="/dashboard/aluno/10/turmas" ><i class="fas fa-users"></i> Turmas</a>
            <a href="dashboard-atividades.html" class="active"><i class="fas fa-file-alt"></i> Atividades</a>
            <a href="#"><i class="fas fa-chart-bar"></i> Ranking</a>
    <%- include('./partials/aside-bottom.ejs') %>
    <div class="main">
        <h2>Dashboard / Turmas / <%= classes.titulo %> / Atividade / <%= data.Atividade.titulo %></h2>
        <hr />
        <div class="tables">
            <table id="data-table">
                <thead>
                    <tr>
                        <th>Atividade</th>
                        <th>Descrição</th>
                        <th>Pontos</th>
                        <th>Prazo</th>
                        <th>Status</th>
                    </tr>
                </thead>
                <tbody>
                    <tr>
                        <td><%= data.Atividade.titulo %></td>
                        <td><%= data.Atividade.descricao %></td>
                        <td><%= data.Atividade.pontuacao %></td>
                        <td><%= data.Atividade.dataTermino %></td>
                    <% if(data.status != null) { %>
                        <td>Corrigido</td>
                    <% } else { %>
                        <td>Pendente</td>
                    <% } %>
                    </tr>
                </tbody>
            </table>
        </div>
        <h4>Resolução da atividade</h4>
        <form class="form" method="POST" action="/dashboard/aluno/<%= student.idUser %>/turmas/<%= classes.idTurma %>/atividade/<%= data.id %>">
            <div>
                <span>
                    <textarea name="textField" placeholder="Digite aqui sua resposta para a atividade" rows="10" cols="77" style="max-width: 95%;"></textarea>
                </span>
            </div>
            <button class="submit" type="submit">Enviar Atividade</button>
        </form>
        <div class="notification">
            <img src="/images/dashboard-aluno/avatar.png" class="avatar" alt="avatar">
            <span class="badge">🔔</span>
        </div>
    </div>
</body>
</html>