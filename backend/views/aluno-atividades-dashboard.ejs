<!DOCTYPE html>
<html lang="pt-br">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="/stylesheets/global.css">
    <link rel="shortcut icon" href="/images/dashboard-aluno/favicon.png" />
    <link rel="stylesheet" href="/stylesheets/dashboard-aluno/estiloa1.css">
    <script defer src="/fa/brands.js"></script>
    <script defer src="/fa/solid.js"></script>
    <script defer src="/fa/fontawesome.js"></script>
    <title>elevel - Dashboard Aluno</title>
</head>
<body>
    <%- include('./partials/aside-top.ejs') %>
            <a href="/dashboard/aluno/10"><i class="fas fa-graduation-cap"></i> Aluno</a>
            <a href="/dashboard/aluno/10/turmas" ><i class="fas fa-users"></i> Turmas</a>
            <a href="dashboard-atividades.html" class="active"><i class="fas fa-file-alt"></i> Atividades</a>
            <a href="#"><i class="fas fa-chart-bar"></i> Ranking</a>
    <%- include('./partials/aside-bottom.ejs') %>
    <div class="main">
        <h2>Dashboard / Turmas / <%= classes.titulo %> / Atividades</h2>
        <hr />
        <div class="billboard">
            <a href="#menu-toggle" class="close-bilboard btn-default"><i class="fas fa-times"></i></a>
            <h3><%= student.nome %>,</h3>
            <p>Aqui você tem acesso às atividades que já se registrou e outras atividades que estão disponíveis para a turma de <%= classes.titulo %>, praticando todos os conceitos das aulas.</p>
        </div>
        <h4>Atividades que estou registrado</h4>
        <div class="tables">
            <table id="data-table">
                <thead>
                    <tr>
                        <th>Atividade</th>
                        <th>Descrição</th>
                        <th>Pontos</th>
                        <th>Prazo</th>
                        <th>Status</th>
                    </tr>
                </thead>
                <tbody>
                    <% subscribed.forEach(subRow => { %>
                    <tr onclick="window.location='/dashboard/aluno/<%= student.idUser %>/turmas/<%= classes.idTurma %>/atividade/<%= subRow.id %>'">
                        <td><%= subRow.Atividade.titulo %></td>
                        <td><%= subRow.Atividade.descricao %></td>
                        <td><%= subRow.Atividade.pontuacao %></td>
                        <td><%= subRow.Atividade.dataTermino %></td>
                    <% if(subRow.textField != null && subRow.textField != '') { %>
                        <td>Enviado</td>
                    <% } else { %>
                        <td>Pendente</td>
                    <% } %>
                    </tr>
                </tbody>
            </table>
            <h4>Novas atividades para a turma</h4>
            <table id="data-table">
                <thead>
                    <tr>
                        <th>Atividade</th>
                        <th>Descrição</th>
                        <th>Pontos</th>
                        <th>Prazo</th>
                        <th>Status</th>
                    </tr>
                </thead>
                <tbody>
                <% data.forEach(row => { %>
                    <% if(!(row.Atividade.idAtividade == subRow.Atividade.idAtividade)) { %>
                    <tr>
                        <td><%= row.Atividade.titulo %></td>
                        <td><%= row.Atividade.descricao %></td>
                        <td><%= row.Atividade.pontuacao %></td>
                        <td><%= row.Atividade.dataTermino %></td>
                        <td>Registrar</td>
                    </tr>
                <% } }) })%>
                </tbody>
            </table>
        </div>
        <div class="notification">
            <img src="/images/dashboard-aluno/avatar.png" class="avatar" alt="avatar">
            <span class="badge">🔔</span>
        </div>
    </div>
</body>
</html>